/**
 * shared/region.js
 *
 * Регионы, районы и логика гео-матчинга.
 *
 * Особенность матчинга: последние 3 буквы названия района/области
 * могут быть любыми или отсутствовать — используем усечение основы.
 * Пример: "Гомельск" совпадает с "Гомельская", "Гомельского" и т.д.
 */

import { matchKeywords } from "./matchKeyword.js";

export const REGIONS = [
  "Брестская",
  "Витебская",
  "Гомельская",
  "Гродненская",
  "Минская",
  "Могилёвская",
];

// Районы по областям (без слова "район" — оно добавляется при матчинге)
export const DISTRICTS = {
  "Брестская": [
    "Барановичский","Берёзовский","Брестский","Ганцевичский","Дрогичинский",
    "Жабинковский","Ивановский","Ивацевичский","Кобринский","Лунинецкий",
    "Ляховичский","Малоритский","Пинский","Пружанский","Столинский",
  ],
  "Витебская": [
    "Бешенковичский","Браславский","Верхнедвинский","Витебский","Глубокский",
    "Городокский","Докшицкий","Дубровенский","Лепельский","Лиозненский",
    "Миорский","Оршанский","Полоцкий","Поставский","Россонский",
    "Сенненский","Толочинский","Ушачский","Чашникский","Шарковщинский",
    "Шумилинский",
  ],
  "Гомельская": [
    "Брагинский","Буда-Кошелёвский","Ветковский","Гомельский","Добрушский",
    "Ельский","Житковичский","Жлобинский","Калинковичский","Кормянский",
    "Лельчицкий","Лоевский","Мозырский","Наровлянский","Октябрьский",
    "Петриковский","Речицкий","Рогачёвский","Светлогорский","Хойникский",
    "Чечерский",
  ],
  "Гродненская": [
    "Берестовицкий","Вороновский","Гродненский","Дятловский","Зельвенский",
    "Ивьевский","Кореличский","Лидский","Мостовский","Новогрудский",
    "Островецкий","Ошмянский","Свислочский","Скидельский","Слонимский",
    "Сморгонский","Щучинский",
  ],
  "Минская": [
    "Березинский","Борисовский","Вилейский","Воложинский","Дзержинский",
    "Клецкий","Копыльский","Крупский","Логойский","Любанский",
    "Минский","Молодечненский","Мядельский","Несвижский","Пуховичский",
    "Слуцкий","Смолевичский","Солигорский","Стародорожский","Столбцовский",
    "Узденский","Червенский",
  ],
  "Могилёвская": [
    "Белыничский","Бобруйский","Быховский","Горецкий","Дрибинский",
    "Кировский","Климовичский","Кличевский","Костюковичский","Краснопольский",
    "Кричевский","Круглянский","Могилёвский","Мстиславский","Осиповичский",
    "Славгородский","Хотимский","Чаусский","Чериковский","Шкловский",
  ],
};

// ── Метки ─────────────────────────────────────────────────────

export function regionLabel(r) {
  if (!r || r === "all")      return "🌍 Вся страна";
  if (r === "keywords")       return "🔤 По ключевым словам";
  if (Array.isArray(r))       return r.join(", ");
  return r;
}

// ── Матчинг ───────────────────────────────────────────────────

/**
 * Возвращает основу слова для нечёткого совпадения:
 * отрезает последние 3 буквы (но не меньше 4 символов остатка).
 */
/**
 * Нечёткий матчинг гео-названия с учётом склонений.
 * Алгоритм:
 *   - Слова ≤4 букв: отрезаем 1 букву (Лида→Лид)
 * Снимает адъективные окончания: "Речицкий"→"Речиц", "Жлобинский"→"Жлобин".
 * После основы разрешаем 0–5 любых кириллических символов.
 * Логика ИЛИ: вызывающий код использует .some() по массиву токенов.
 */
export function stemGeo(word) {
  const s = word.trim();
  const endings = [
    "ского","ской","скому","ским","ское","ских",
    "ский","ская",
    "кого","ком","кой","ких","кому","ким",
    "кий","кая","кое","кие",
    "ого","ой","ому","им","ые","ий","ая","ое",
  ];
  for (const end of endings) {
    if (s.toLowerCase().endsWith(end) && s.length - end.length >= 3) {
      return s.slice(0, s.length - end.length);
    }
  }
  // Для топонимов (Тереховка→Терехов, Гомель→Гомел):
  if (s.length <= 5) return s;
  return s.slice(0, -2);
}

/**
 * Проверяет попадание текста по одному гео-токену.
 * После основы допускаем 0–5 любых кириллических символов.
 */
export function matchGeoToken(locationText, token) {
  const stem = stemGeo(token);
  const pattern = new RegExp(
    stem.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '[а-яёА-ЯЁ]{0,5}',
    'i'
  );
  return pattern.test(locationText);
}

/**
 * Проверяет подходит ли locationText под регион подписки.
 *
 * sub.region:
 *   "all"                → всегда true
 *   "keywords"           → матчинг через sub.regionKeywords (matchKeywords)
 *   ["Гомельская"]       → нечёткий матчинг по стемам названий
 *
 * sub.regionDistricts:   массив строк — районы (нечёткий матчинг)
 * sub.regionCouncil:     строка — сельсовет/горсовет (точное вхождение)
 */
export function matchRegion(region, locationText, regionKeywords, sub) {
  if (!region || region === "all") return true;

  const loc = (locationText || "").toLowerCase();

  if (region === "keywords") {
    if (!regionKeywords || regionKeywords.length === 0) return true;
    return matchKeywords(loc, regionKeywords);
  }

  // Область — нечёткий матчинг
  const regions = Array.isArray(region) ? region : [region];
  const oblastMatch = regions.some(r => matchGeoToken(loc, r));
  if (!oblastMatch) return false;

  // Районы — если выбраны, хотя бы один должен совпасть
  const districts = sub?.regionDistricts;
  if (districts?.length > 0) {
    const districtMatch = districts.some(d => matchGeoToken(loc, d));
    if (!districtMatch) return false;
  }

  // Сельсовет — если указан, должен встречаться (частичное совпадение)
  // Населённый пункт — OR по запятым, частичное нечёткое совпадение
  const council = sub?.regionCouncil;
  if (council) {
    const places = council.split(/[,;]+/).map(s => s.trim().toLowerCase()).filter(Boolean);
    if (places.length > 0) {
      const councilMatch = places.some(p => {
        const stem = stemGeo(p);
        return new RegExp(
          stem.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '[а-яёА-ЯЁ]{0,4}', 'i'
        ).test(loc);
      });
      if (!councilMatch) return false;
    }
  }

  return true;
}
